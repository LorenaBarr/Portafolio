<header>
  <button class="menu-toggle" aria-label="Abrir menú"> ☰ </button>
  <nav>
    <ul class="nav-list">
      <li><a href="#inicio">Inicio</a></li>
      <li><a href="#proyectos">Proyectos</a></li>
      <li><a href="#educacion">Educación</a></li>
      <li><a href="#sobre-mi">Sobre mí</a></li>
    </ul>
  </nav>
</header>

<style>
  header {
    background-color: #333; /* Tono más claro que el resto del fondo */
    padding: 1rem; /* Espaciado interno */
  }

  .nav-list {
    display: flex;
    justify-content: center;
    gap: 2rem; /* Espacio entre los elementos del menú */
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-list li {
    display: inline;
  }

  .nav-list a {
    color: #fff; /* Color del texto de los enlaces */
    text-decoration: none;
    font-size: 1.2rem;
    position: relative; /* Necesario para la pseudo-clase :after */
  }

  /* Línea de neón debajo del enlace al hacer hover */
  .nav-list a::after {
    content: "";
    position: absolute;
    width: 100%;
    height: 3px;
    left: 0;
    bottom: -5px;
    background: linear-gradient(
      90deg,
      #000000,
      #fa448c,
      #491d88
    ); /* Degradado de la línea de neón */
    transition: width 0.3s ease-in-out;
    width: 0;
  }

  .nav-list a:hover::after {
    width: 100%; /* La línea se muestra al pasar el mouse */
  }

  /* Estilos específicos para pantallas pequeñas (móviles) */
  @media (max-width: 768px) {
    .nav-list {
      display: none; /* Oculta el menú por defecto */
      flex-direction: column;
      align-items: center;
    }

    .nav-list.open {
      display: flex; /* Muestra el menú cuando está abierto */
    }

    .menu-toggle {
      display: block; /* Muestra el botón de menú en móviles */
      background: none;
      border: none;
      color: white;
      font-size: 2rem;
      cursor: pointer;
    }
  }

  /* Ocultar el botón del menú hamburguesa en pantallas grandes */
  @media (min-width: 769px) {
    .menu-toggle {
      display: none; /* Oculta el botón de menú en pantallas grandes */
    }
  }
</style>
